# Development-only dependencies. Not installed in the production
# Docker image — only CI and local contributor setups.
#
# Install with:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0
pytest-cov>=5.0
coverage[toml]>=7.6
httpx>=0.27

# Lint
ruff==0.6.9

# Browser E2E
playwright>=1.48
pytest-playwright>=0.5
